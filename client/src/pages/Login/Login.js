import React, {Component} from "react";
import { Grid, Row, Col } from 'react-bootstrap';
import FontAwesome from 'react-fontawesome';
import "./Login.css";

const Login = props => (
    <Row>
        <Col xs={12}>This is the login page.</Col>
    </Row>

);

export default Login;